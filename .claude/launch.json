{
  "_comment": "Windows fix: invoke node directly against next's bin to bypass npm/npx .cmd shim issues with the preview wrapper. Mirror in workspace root C:/Vortik.dev/.claude/launch.json with cwd set, since the preview wrapper runs from workspace root.",
  "version": "0.0.1",
  "configurations": [
    {
      "name": "neto-landing",
      "runtimeExecutable": "node",
      "runtimeArgs": ["./node_modules/next/dist/bin/next", "dev", "--port", "3000"],
      "port": 3000,
      "autoPort": false
    }
  ]
}
